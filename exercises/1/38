(define (e k)
  (+ 2 (cont-frac (lambda (i) 1.0)
                  (lambda (i) (if (= 2 (remainder i 3))
                                  (* 2 (ceiling (/ i 3)))
                                  1))
                  k)
