(define (smooth f dx)
  (lambda (x) (/ (+ (f (- x dx))
                    (f x)
                    (f (+ x dx)))
               3)))

(define (smooth-n f dx n)
  (define (smooth-one f)
    (smooth f dx))
  ((repeated smooth-one n) f))
